import { LanguageClient } from 'coc.nvim';
import { CancellationToken } from 'vscode-languageserver-protocol';
import { URI } from 'vscode-uri';
export declare namespace WorkspaceDiscovery {
    var client: LanguageClient;
    var maxFileSizeBytes: number;
    function checkCacheThenDiscover(uriArray: URI[], checkModTime: boolean, token: CancellationToken): Promise<number>;
    function discover(uriArray: URI[], token?: CancellationToken): Promise<number>;
    function delayedDiscover(uri: URI): void;
    function delayedDiscoverFlush(): void;
    function forget(uri: URI): Promise<void>;
}
